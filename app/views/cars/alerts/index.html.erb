<div class="alert-page">
  <div>
    <div class="d-flex alert-return-link">
      <i class="fas fa-angle-left return-icon-alert"></i>
      <%= link_to "Retour au véhicule", car_path(@car), class: "return-text"  %>
    </div>
  </div>
  <div>
    <% @alerts.each do |alert| %>
    <!-- show all alerts -->
      <%= render 'components/alert_category/card', alert: alert %>
    <% end %>
  </div>
</div>
